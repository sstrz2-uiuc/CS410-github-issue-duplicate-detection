[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dedupe-detector"
version = "0.1.0"
description = "Semantic duplicate detection for GitHub issues"
requires-python = ">=3.9"
